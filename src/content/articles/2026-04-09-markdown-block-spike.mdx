---
title: 'MarkdownBlock Test Fixture'
slug: markdown-block-test
draft: true
description: 'Dummy article for testing the MarkdownBlock component.'
pubDate: 2026-04-09
tags: ['test']
---

A draft article for manually testing the `MarkdownBlock` component. It stays
`draft: true` so it never ships to production — visible only in `bun run dev`.

## Paragraph before the block

Here's a regular paragraph with a [link to example.com](https://example.com)
and some **bold** text — included so we can see that surrounding prose still
flows normally around each block.

## 1. Kitchen sink — defaults

This block uses the default view (rendered) and shows most GFM features.

```md preview title="kitchen-sink.md"
# H1 heading

## H2 heading

### H3 heading

A paragraph with **bold**, _italic_, `inline code`, and a
[link to example.com](https://example.com) plus an
[internal link](/notes).

> A blockquote with a cited source.
>
> — Someone, somewhere

- Unordered list item one
- Unordered list item two
  - Nested item
- Unordered list item three

1. Ordered item one
2. Ordered item two
3. Ordered item three

- [ ] Unchecked task
- [x] Checked task

| Column A | Column B |
| -------- | -------- |
| Cell 1   | Cell 2   |
| Cell 3   | Cell 4   |

A nested fenced code block (renders as a plain `<pre><code>` inside the
preview — expressive-code only applies in the source pane):

    function hello(name) {
      return `Hello, ${name}!`;
    }

---

Final paragraph of the included chunk.
```

## 2. Small, no title

```md preview
A tiny example.

Just a paragraph and a list:

- one
- two
- three
```

## 3. Default view = source

Same content, but the block opens on the source pane. Toggle to switch.

```md preview title="source-first.md" defaultView="source"
## This block starts on the source view

Click "Preview" in the header to switch to the rendered view.
```

## 4. README-style with nested fences

The outer fence uses **four** backticks so the included markdown can contain
triple-backtick fences of its own — the whole reason this component exists.

````md preview title="README.md"
# my-project

A made-up project for testing.

## Installation

```bash
npm install my-project
```

## Usage

```js
import { thing } from 'my-project';
thing();
```

## License

MIT
````

## Closing paragraph

If every block above toggles cleanly, copies to the clipboard, and the
rendered pane has prose typography in both themes — we're done.
