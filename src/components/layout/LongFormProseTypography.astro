---
export interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className, ...props } = Astro.props;
---

<div class={`article cq ${className || ''}`} {...props}>
  <slot />
</div>