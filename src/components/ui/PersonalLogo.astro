---
// Personal logo component for site author identification
---

<section class="personal-logo">
  <div class="circle"></div>
  <div class="name">Danny Smith</div>
</section>

<style>
  .personal-logo {
    display: flex;
    align-items: center;
    gap: var(--space-4);
  }

  .circle {
    width: clamp(1rem, calc(0.8rem + 1vw), 1.5rem);
    height: clamp(1rem, calc(0.8rem + 1vw), 1.5rem);
    background-color: var(--color-nav-border);
    border-radius: var(--radius-full);
  }

  .name {
    text-transform: uppercase;
    line-height: 1;
    letter-spacing: var(--tracking-wide);
    font-weight: var(--font-weight-bold);
    text-align: left;
  }
</disabled-style>