---
import StyleguideLayout from './_layout/StyleguideLayout.astro';
---

<StyleguideLayout
  title="HTML Baseline"
  description="Orphan native elements under our default CSS, as a baseline audit."
  current="html"
>
  <header>
    <p>
      Raw native element defaults under our CSS — the orphans without a natural component home
      (forms, <code>&lt;dl&gt;</code>, <code>&lt;figure&gt;</code>). Elements that do have a home
      (media, <code>&lt;details&gt;</code>) live alongside their component instead.
    </p>
  </header>

  <p><em>Scaffolded in Phase 0 — content migrates here in Phase 5.</em></p>
</StyleguideLayout>
