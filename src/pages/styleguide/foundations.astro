---
import StyleguideLayout from './_layout/StyleguideLayout.astro';
import SGSwitcher from './_components/SGSwitcher.astro';
import SGTypeSpecimen from './_components/SGTypeSpecimen.astro';
import { ColorSwatch } from '@components/mdx/index';

const toc = [
  { id: 'colour', label: 'Colour System' },
  { id: 'borders', label: 'Borders' },
  { id: 'font-families', label: 'Typefaces' },
  { id: 'font-sizes', label: 'Font sizes' },
  { id: 'spacing', label: 'Spacing' },
  { id: 'font-weights', label: 'Font weights' },
  { id: 'line-height', label: 'Line height' },
  { id: 'letter-spacing', label: 'Letter spacing' },
  { id: 'utility-classes', label: 'Utility classes' },
];

const capitalise = (s: string) => s.charAt(0).toUpperCase() + s.slice(1);
---

<StyleguideLayout
  title="Foundations"
  description="The design system documented: colour, design tokens, and utility classes."
  current="foundations"
  toc={toc}
>
  <header>
    <p>
      This page is the primary reference for the site's foundational design system. It includes the
      colour system, the design tokens for spacing, type, borders, radii and shadows, and the global
      utility classes.
    </p>
  </header>

  <section id="colour" class="flow">
    <h2>Colour System</h2>
    <p>
      The colour system is rooted in a set of foundational colours, some of which are <strong
        >adaptive</strong
      > and appear differently in light and dark modes (via <code>light-dark()</code>), and some of
      which are absolute and appear the same in both modes. These are used to create a set of <strong
        >semantic</strong
      > role-based tokens that are primarily used across the site. All colours are defined in <a
        href="https://oklch.com/">OKLCH</a
      >. The adaptive colours use lower lightness and higher chroma in light mode and have increased
      lightness and lower chroma in dark mode.
    </p>
    <div class="sg-panel">
      <div class="sg-swatch-grid" style="--sg-swatch-min: 8rem">
        {
          ['white', 'black', 'ink', 'charcoal', 'beige'].map(colour => (
            <ColorSwatch color={`var(--color-${colour})`} name={capitalise(colour)} />
          ))
        }
      </div>
    </div>
    <div class="sg-theme-pair">
      {
        ['light', 'dark'].map(theme => (
          <div class="sg-theme-col flow" data-theme={theme}>
            <p class="sg-theme-label">{theme} mode</p>
            <div class="sg-swatch-grid">
              {['coral', 'pink', 'orange', 'purple', 'yellow', 'green', 'blue'].map(colour => (
                <ColorSwatch color={`var(--color-${colour})`} name={capitalise(colour)} />
              ))}
            </div>
          </div>
        ))
      }
    </div>
    <p>
      The table below shows the semantic colours currently in use. Components should use these (or
      derivatives of them via CSS functions like `color-mix()`) wherever their use case fits their
      semantic role. In situations where a foundational colour is <em>more semantic</em> (eg a "blue callout")
      then use the foundational colour instead. I deliberately try to keep these semantic colours to a
      minimum to avoid ending up with a large unwieldy palette of very specific colours that are only
      used in one place.
    </p>
    <table>
      <thead>
        <tr>
          <th>Token</th>
          <th class="center">Light</th>
          <th class="center">Dark</th>
          <th>Variable</th>
          <th>Usage</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th colspan="5">Accent & interaction</th>
        </tr>
        <tr>
          <td>Accent</td>
          <td class="center" data-theme="light">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-accent)" />
            </span>
          </td>
          <td class="center" data-theme="dark">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-accent)" />
            </span>
          </td>
          <td>
            <code>--color-accent</code>
          </td>
          <td>The primary brand colour, used in many places including for links</td>
        </tr>
        <tr>
          <td>Visited</td>
          <td class="center" data-theme="light">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-visited)" />
            </span>
          </td>
          <td class="center" data-theme="dark">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-visited)" />
            </span>
          </td>
          <td>
            <code>--color-visited</code>
          </td>
          <td>Visited links, because purple is an affordance people are used to</td>
        </tr>
        <tr>
          <td>Highlight</td>
          <td class="center" data-theme="light">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-highlight)" />
            </span>
          </td>
          <td class="center" data-theme="dark">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-highlight)" />
            </span>
          </td>
          <td>
            <code>--color-highlight</code>
          </td>
          <td>Highlighted text and occasionally other forms of highlight</td>
        </tr>
        <tr>
          <td>Focus ring</td>
          <td class="center" data-theme="light">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-focus-ring)" />
            </span>
          </td>
          <td class="center" data-theme="dark">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-focus-ring)" />
            </span>
          </td>
          <td>
            <code>--color-focus-ring</code>
          </td>
          <td>The focus ring for interactive elements</td>
        </tr>
      </tbody>
      <tbody>
        <tr>
          <th colspan="5">Backgrounds & surfaces</th>
        </tr>
        <tr>
          <td>Primary background</td>
          <td class="center" data-theme="light">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-background)" />
            </span>
          </td>
          <td class="center" data-theme="dark">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-background)" />
            </span>
          </td>
          <td>
            <code>--color-background</code>
          </td>
          <td>The primary background colour</td>
        </tr>
        <tr>
          <td>Secondary background</td>
          <td class="center" data-theme="light">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-background-secondary)" />
            </span>
          </td>
          <td class="center" data-theme="dark">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-background-secondary)" />
            </span>
          </td>
          <td>
            <code>--color-background-secondary</code>
          </td>
          <td>A secondary background colour for things like cards and panels</td>
        </tr>
        <tr>
          <td>Code background</td>
          <td class="center" data-theme="light">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-background-code)" />
            </span>
          </td>
          <td class="center" data-theme="dark">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-background-code)" />
            </span>
          </td>
          <td>
            <code>--color-background-code</code>
          </td>
          <td>Background for inline code and some other code-like elements</td>
        </tr>
      </tbody>
      <tbody>
        <tr>
          <th colspan="5">Text & borders</th>
        </tr>
        <tr>
          <td>Text</td>
          <td class="center" data-theme="light">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-text)" />
            </span>
          </td>
          <td class="center" data-theme="dark">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-text)" />
            </span>
          </td>
          <td>
            <code>--color-text</code>
          </td>
          <td>Body text</td>
        </tr>
        <tr>
          <td>Text secondary</td>
          <td class="center" data-theme="light">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-text-secondary)" />
            </span>
          </td>
          <td class="center" data-theme="dark">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-text-secondary)" />
            </span>
          </td>
          <td>
            <code>--color-text-secondary</code>
          </td>
          <td>Muted or secondary text</td>
        </tr>
        <tr>
          <td>Border</td>
          <td class="center" data-theme="light">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-border)" />
            </span>
          </td>
          <td class="center" data-theme="dark">
            <span class="sg-mini-swatch">
              <ColorSwatch color="var(--color-border)" />
            </span>
          </td>
          <td>
            <code>--color-border</code>
          </td>
          <td>Subtle borders and dividers</td>
        </tr>
      </tbody>
    </table>
  </section>

  <section id="design-tokens" class="flow">
    <h2>Design tokens</h2>
    <p>
      As with colours, I've tried to keep the number of generic design tokens to a minimum, while
      still providing a wide enough set of options for most use cases.
    </p>

    <h3 id="borders">Borders, radii and shadows</h3>
    <p>
      Five CSS custom properties are available for setting border widths and radii. Border widths
      are set in pixels so they don't scale with the base font size while radii are mostly set in
      rems so they do.
    </p>
    <table>
      <thead>
        <tr>
          <th>Variable</th>
          <th>Value</th>
          <th>Preview</th>
          <th>Notes</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>--border-width-hairline</code></td>
          <td><code>max(0.0625rem, 1px)</code></td>
          <td>
            <div
              class="border-width-preview"
              style="border-bottom-width: var(--border-width-hairline)"
            >
            </div>
          </td>
          <td
            >1px at the default root size but never falls to a sub-pixel when scaling up with the
            root font size</td
          >
        </tr>
        <tr>
          <td><code>--border-width-base</code></td>
          <td><code>2px</code></td>
          <td>
            <div class="border-width-preview" style="border-bottom-width: var(--border-width-base)">
            </div>
          </td>
          <td>Standard border width for most elements</td>
        </tr>
        <tr>
          <td><code>--border-width-thick</code></td>
          <td><code>4px</code></td>
          <td>
            <div
              class="border-width-preview"
              style="border-bottom-width: var(--border-width-thick)"
            >
            </div>
          </td>
          <td>Usually only applied to one edge of an element</td>
        </tr>
        <tr>
          <td><code>--border-width-heavy</code></td>
          <td><code>6px</code></td>
          <td>
            <div
              class="border-width-preview"
              style="border-bottom-width: var(--border-width-heavy)"
            >
            </div>
          </td>
          <td>Usually only applied to one edge of an element</td>
        </tr>
        <tr>
          <td><code>--border-width-accent</code></td>
          <td><code>1rem</code></td>
          <td>
            <div
              class="border-width-preview"
              style="border-bottom-width: var(--border-width-accent)"
            >
            </div>
          </td>
          <td>Used as a design element, so scales with the root font size</td>
        </tr>
      </tbody>
    </table>
    <table>
      <thead>
        <tr>
          <th>Variable</th>
          <th>Value</th>
          <th>Preview</th>
          <th>Notes</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>--radius-xs</code></td>
          <td><code>0.125rem</code></td>
          <td><div class="border-radius-preview" style="border-radius: var(--radius-xs)"></div></td>
          <td>Very subtle radius for softening corners slightly</td>
        </tr>
        <tr>
          <td><code>--radius-sm</code></td>
          <td><code>0.25rem</code></td>
          <td><div class="border-radius-preview" style="border-radius: var(--radius-sm)"></div></td>
          <td>Standard radius for most rounded elements</td>
        </tr>
        <tr>
          <td><code>--radius-md</code></td>
          <td><code>0.5rem</code></td>
          <td><div class="border-radius-preview" style="border-radius: var(--radius-md)"></div></td>
          <td></td>
        </tr>
        <tr>
          <td><code>--radius-lg</code></td>
          <td><code>0.75rem</code></td>
          <td><div class="border-radius-preview" style="border-radius: var(--radius-lg)"></div></td>
          <td></td>
        </tr>
        <tr>
          <td><code>--radius-full</code></td>
          <td><code>calc(infinity * 1px)</code></td>
          <td>
            <div class="border-radius-preview" style="border-radius: var(--radius-full)"></div>
          </td>
          <td>Infinite radius for fully rounding into a pill or circle regardless of size.</td>
        </tr>
      </tbody>
    </table>
    <p>
      I don't use shadows heavily but there are two custom properties available if I need to create
      depth and elevation.
    </p>
    <table>
      <thead>
        <tr>
          <th>Variable</th>
          <th>Preview</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>--shadow-small</code></td>
          <td class="shadow-preview-container">
            <div class="shadow-preview" style="filter: var(--shadow-small)"></div>
          </td>
        </tr>
        <tr>
          <td><code>--shadow-medium</code></td>
          <td class="shadow-preview-container">
            <div class="shadow-preview" style="filter: var(--shadow-medium)"></div>
          </td>
        </tr>
      </tbody>
    </table>
  </section>

  <section id="typography-spacing" class="flow">
    <h2>Typography & Spacing Tokens</h2>
    <p>
      Typography and spacing designtokens provide consistent rhythm and visual hireachy across the site.
    </p>

    <h3 id="font-families">Font Families</h3>
    <p>
      Typography on this site serves five distinct usage contexts, covered by four font stacks. Short-form prose and UI elements use Figtree (<code>--font-ui</code>) which works well for both use cases and is the deault face. Long-form prose uses Literata (<code>--font-prose</code>), while "display" type uses Giest (<code>--font-display</code>). Code examples and other monospacefonts use a version of Fira Code.
    </p>
    <div class="sg-type-specimens">
      <SGTypeSpecimen
        family="Geist"
        url="https://vercel.com/font"
        usageTitle="Display"
        cssVar="--font-display"
        description="Large, presentational type. Page titles, hero text, 404, brand marks. Mostly used as Caps."
      />
      <SGTypeSpecimen
        family="Literata"
        url="https://fonts.google.com/specimen/Literata"
        usageTitle="Long-form prose"
        cssVar="--font-prose"
        description="Articles and any content inside .longform-prose. Rich typographic features for sustained reading."
      />
      <SGTypeSpecimen
        family="Figtree"
        url="https://fonts.google.com/specimen/Figtree"
        usageTitle="Short-form prose & UI"
        cssVar="--font-ui"
        description="Document body default. Notes, /now, callouts, captions AND interface elements — navigation, buttons, pills, form controls."
      />
      <SGTypeSpecimen
        family="Fira Code"
        url="https://github.com/tonsky/FiraCode"
        usageTitle="Code"
        cssVar="--font-code"
        description="Code blocks, inline code, and occasional stylistic monospace."
      />
    </div>

    <h3 id="font-sizes">Font Sizes</h3>
    <p>Fluid type scale using <a href="https://utopia.fyi/type/calculator">Utopia</a>.</p>
    <table>
      <thead>
        <tr>
          <th>Variable</th>
          <th>Value</th>
          <th>Preview</th>
          <th>Notes</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>--font-size-xs</code></td>
          <td><code>var(--step--2)</code></td>
          <td class="font-size-preview" style="font-size: var(--font-size-xs)">Aa</td>
          <td>Captions, labels, fine print</td>
        </tr>
        <tr>
          <td><code>--font-size-sm</code></td>
          <td><code>var(--step--1)</code></td>
          <td class="font-size-preview" style="font-size: var(--font-size-sm)">Aa</td>
          <td>Secondary text, metadata, UI elements</td>
        </tr>
        <tr>
          <td><code>--font-size-base</code></td>
          <td><code>var(--step-0)</code></td>
          <td class="font-size-preview" style="font-size: var(--font-size-base)">Aa</td>
          <td>Body text, paragraphs</td>
        </tr>
        <tr>
          <td><code>--font-size-md</code></td>
          <td><code>var(--step-1)</code></td>
          <td class="font-size-preview" style="font-size: var(--font-size-md)">Aa</td>
          <td>Emphasized body text, lead paragraphs</td>
        </tr>
        <tr>
          <td><code>--font-size-lg</code></td>
          <td><code>var(--step-2)</code></td>
          <td class="font-size-preview" style="font-size: var(--font-size-lg)">Aa</td>
          <td>Section headings (h4), large UI text</td>
        </tr>
        <tr>
          <td><code>--font-size-xl</code></td>
          <td><code>var(--step-3)</code></td>
          <td class="font-size-preview" style="font-size: var(--font-size-xl)">Aa</td>
          <td>Subheadings (h3)</td>
        </tr>
        <tr>
          <td><code>--font-size-2xl</code></td>
          <td><code>var(--step-4)</code></td>
          <td class="font-size-preview" style="font-size: var(--font-size-2xl)">Aa</td>
          <td>Major headings (h2)</td>
        </tr>
        <tr>
          <td><code>--font-size-3xl</code></td>
          <td><code>var(--step-5)</code></td>
          <td class="font-size-preview" style="font-size: var(--font-size-3xl)">Aa</td>
          <td>Page titles (h1)</td>
        </tr>
      </tbody>
    </table>

    <h3 id="spacing">Spacing Scale</h3>
    <p>
      Generated using <a href="https://utopia.fyi/space/calculator">Utopia</a>. Base scale provides
      consistent spacing; fluid pairs smoothly interpolate between sizes based on viewport.
    </p>
    <table>
      <thead>
        <tr>
          <th>Variable</th>
          <th>Value</th>
          <th>Preview</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>--space-3xs</code></td>
          <td><code>clamp(0.25rem, 0.2241rem + 0.1105vw, 0.3125rem)</code></td>
          <td><div class="spacing-bar" style="width: var(--space-3xs)"></div></td>
        </tr>
        <tr>
          <td><code>--space-2xs</code></td>
          <td><code>clamp(0.5rem, 0.4482rem + 0.221vw, 0.625rem)</code></td>
          <td><div class="spacing-bar" style="width: var(--space-2xs)"></div></td>
        </tr>
        <tr>
          <td><code>--space-xs</code></td>
          <td><code>clamp(0.75rem, 0.6723rem + 0.3315vw, 0.9375rem)</code></td>
          <td><div class="spacing-bar" style="width: var(--space-xs)"></div></td>
        </tr>
        <tr>
          <td><code>--space-s</code></td>
          <td><code>clamp(1rem, 0.8964rem + 0.442vw, 1.25rem)</code></td>
          <td><div class="spacing-bar" style="width: var(--space-s)"></div></td>
        </tr>
        <tr>
          <td><code>--space-m</code></td>
          <td><code>clamp(1.5rem, 1.3446rem + 0.663vw, 1.875rem)</code></td>
          <td><div class="spacing-bar" style="width: var(--space-m)"></div></td>
        </tr>
        <tr>
          <td><code>--space-l</code></td>
          <td><code>clamp(2rem, 1.7928rem + 0.884vw, 2.5rem)</code></td>
          <td><div class="spacing-bar" style="width: var(--space-l)"></div></td>
        </tr>
        <tr>
          <td><code>--space-xl</code></td>
          <td><code>clamp(3rem, 2.6892rem + 1.326vw, 3.75rem)</code></td>
          <td><div class="spacing-bar" style="width: var(--space-xl)"></div></td>
        </tr>
        <tr>
          <td><code>--space-2xl</code></td>
          <td><code>clamp(4rem, 3.5856rem + 1.768vw, 5rem)</code></td>
          <td><div class="spacing-bar" style="width: var(--space-2xl)"></div></td>
        </tr>
        <tr>
          <td><code>--space-3xl</code></td>
          <td><code>clamp(6rem, 5.3785rem + 2.6519vw, 7.5rem)</code></td>
          <td><div class="spacing-bar" style="width: var(--space-3xl)"></div></td>
        </tr>
      </tbody>
    </table>

    <h3 id="font-weights">Font Weights</h3>
    <p>
      All four families are variable fonts. Exact weight range depends on the face (Geist 100–900,
      Figtree 300–900, Literata 200–900, Fira Code 300–700). These tokens provide common stops — use
      intermediate values when a design calls for it.
    </p>
    <table>
      <thead>
        <tr>
          <th>Variable</th>
          <th>Value</th>
          <th>Preview</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>--font-weight-light</code></td>
          <td><code>300</code></td>
          <td class="font-weight-preview" style="font-weight: var(--font-weight-light)">Aa</td>
        </tr>
        <tr>
          <td><code>--font-weight-normal</code></td>
          <td><code>350</code></td>
          <td class="font-weight-preview" style="font-weight: var(--font-weight-normal)">Aa</td>
        </tr>
        <tr>
          <td><code>--font-weight-regular</code></td>
          <td><code>400</code></td>
          <td class="font-weight-preview" style="font-weight: var(--font-weight-regular)">Aa</td>
        </tr>
        <tr>
          <td><code>--font-weight-medium</code></td>
          <td><code>500</code></td>
          <td class="font-weight-preview" style="font-weight: var(--font-weight-medium)">Aa</td>
        </tr>
        <tr>
          <td><code>--font-weight-semibold</code></td>
          <td><code>600</code></td>
          <td class="font-weight-preview" style="font-weight: var(--font-weight-semibold)">Aa</td>
        </tr>
        <tr>
          <td><code>--font-weight-bold</code></td>
          <td><code>700</code></td>
          <td class="font-weight-preview" style="font-weight: var(--font-weight-bold)">Aa</td>
        </tr>
        <tr>
          <td><code>--font-weight-extrabold</code></td>
          <td><code>800</code></td>
          <td class="font-weight-preview" style="font-weight: var(--font-weight-extrabold)">Aa</td>
        </tr>
        <tr>
          <td><code>--font-weight-heavy</code></td>
          <td><code>900</code></td>
          <td class="font-weight-preview" style="font-weight: var(--font-weight-heavy)">Aa</td>
        </tr>
      </tbody>
    </table>

    <h3 id="line-height">Line Height</h3>
    <p>
      Line height can be controlled via the <code>--leading-*</code> CSS variables. The default is <code
        >var(--leading-normal)</code
      >.
    </p>
    <table>
      <thead>
        <tr>
          <th>Variable</th>
          <th>Value</th>
          <th>Preview</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>--leading-none</code></td>
          <td><code>0.9</code></td>
          <td
            ><p class="leading-preview" style="line-height: var(--leading-none)">
              Lorem ipsum dolor sit amet consectetur adipisicing elit. Quisquam, quos.
            </p></td
          >
        </tr>
        <tr>
          <td><code>--leading-tight</code></td>
          <td><code>1.1</code></td>
          <td
            ><p class="leading-preview" style="line-height: var(--leading-tight)">
              Lorem ipsum dolor sit amet consectetur adipisicing elit. Quisquam, quos.
            </p></td
          >
        </tr>
        <tr>
          <td><code>--leading-snug</code></td>
          <td><code>1.2</code></td>
          <td
            ><p class="leading-preview" style="line-height: var(--leading-snug)">
              Lorem ipsum dolor sit amet consectetur adipisicing elit. Quisquam, quos.
            </p></td
          >
        </tr>
        <tr>
          <td><code>--leading-normal</code></td>
          <td><code>1.55</code></td>
          <td
            ><p class="leading-preview" style="line-height: var(--leading-normal)">
              Lorem ipsum dolor sit amet consectetur adipisicing elit. Quisquam, quos.
            </p></td
          >
        </tr>
        <tr>
          <td><code>--leading-loose</code></td>
          <td><code>1.7</code></td>
          <td
            ><p class="leading-preview" style="line-height: var(--leading-loose)">
              Lorem ipsum dolor sit amet consectetur adipisicing elit. Quisquam, quos.
            </p></td
          >
        </tr>
      </tbody>
    </table>

    <h3 id="letter-spacing">Letter Spacing</h3>
    <p>
      Letter spacing can be controlled via the <code>--tracking-*</code> CSS variables. The default is
      <code>var(--tracking-normal)</code>.
    </p>
    <table>
      <thead>
        <tr>
          <th>Variable</th>
          <th>Value</th>
          <th>Preview</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>--tracking-tight</code></td>
          <td><code>-0.02ch</code></td>
          <td class="letter-spacing-preview" style="letter-spacing: var(--tracking-tight)"
            >Hello there</td
          >
        </tr>
        <tr>
          <td><code>--tracking-normal</code></td>
          <td><code>0</code></td>
          <td class="letter-spacing-preview" style="letter-spacing: var(--tracking-normal)"
            >Hello there</td
          >
        </tr>
        <tr>
          <td><code>--tracking-wide</code></td>
          <td><code>0.05ch</code></td>
          <td class="letter-spacing-preview" style="letter-spacing: var(--tracking-wide)"
            >Hello there</td
          >
        </tr>
        <tr>
          <td><code>--tracking-wider</code></td>
          <td><code>0.1ch</code></td>
          <td class="letter-spacing-preview" style="letter-spacing: var(--tracking-wider)"
            >Hello there</td
          >
        </tr>
      </tbody>
    </table>
  </section>

  <section id="utility-classes" class="flow">
    <h2>Utility Classes</h2>
    <p>
      Global utility classes available throughout the site. Most are documented here for reference;
      <code>.list-reset</code> has a visual demo below.
    </p>

    <table>
      <thead>
        <tr>
          <th>Class</th>
          <th>Purpose</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>.ui-style</code></td>
          <td>
            Opt-out of prose typography for nav, footer, UI-heavy areas. Already demoed extensively
            via SGTypographySwitcher throughout this styleguide.
          </td>
        </tr>
        <tr>
          <td><code>.dark-surface</code></td>
          <td>
            Forces dark background (charcoal) with light text (beige). For always-dark areas
            regardless of theme.
          </td>
        </tr>
        <tr>
          <td><code>.surface-white</code></td>
          <td>
            White surface context (light mode) or raised dark surface (dark mode). Also redefines
            <code>--color-background-secondary</code> for children to ensure contrast.
          </td>
        </tr>
        <tr>
          <td><code>.cq</code></td>
          <td>
            Establishes container query context (<code>container-type: inline-size</code>).
          </td>
        </tr>
        <tr>
          <td><code>.all-caps</code></td>
          <td>Uppercase text with wide letter-spacing. For labels and UI text.</td>
        </tr>
        <tr>
          <td><code>.center</code>, <code>.right</code>, <code>.left</code></td>
          <td>Text alignment utilities. Useful for table cells and other contexts.</td>
        </tr>
        <tr>
          <td><code>.top</code>, <code>.middle</code>, <code>.bottom</code></td>
          <td>Vertical alignment utilities. Useful for table cells and inline elements.</td>
        </tr>
        <tr>
          <td><code>.content-trim</code></td>
          <td>
            Removes top margin from first child, bottom margin from last child. Use inside padded
            containers with slotted content.
          </td>
        </tr>
        <tr>
          <td><code>.img-cover</code></td>
          <td>
            Makes image fill container with <code>object-fit: cover</code>.
          </td>
        </tr>
        <tr>
          <td><code>.sr-only</code></td>
          <td>
            Visually hidden but accessible to screen readers. Also available as
            <code>.hidden-microformat</code> for hiding microformat metadata.
          </td>
        </tr>
        <tr>
          <td><code>.external-arrow</code></td>
          <td>Subtle arrow indicator for external/offsite links in UI contexts.</td>
        </tr>
        <tr>
          <td><code>.list-reset</code></td>
          <td>Removes list styling for navigation/UI lists. See demo below.</td>
        </tr>
      </tbody>
    </table>

    <h3 id="list-reset-demo">.list-reset</h3>
    <p>
      The <code>.list-reset</code> class removes default list styling (bullets, padding) for navigation-style
      lists:
    </p>
    <SGSwitcher>
      <div style="display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-m);">
        <div>
          <strong>Without .list-reset</strong>
          <ul>
            <li><a href="#">Home</a></li>
            <li><a href="#">About</a></li>
            <li><a href="#">Writing</a></li>
            <li><a href="#">Contact</a></li>
          </ul>
        </div>
        <div>
          <strong>With .list-reset</strong>
          <ul class="list-reset">
            <li><a href="#">Home</a></li>
            <li><a href="#">About</a></li>
            <li><a href="#">Writing</a></li>
            <li><a href="#">Contact</a></li>
          </ul>
        </div>
      </div>
    </SGSwitcher>
  </section>
</StyleguideLayout>

<style>
  /* Grid of square colour swatches; --sg-swatch-min controls minimum cell size. */
  .sg-swatch-grid {
    --sg-swatch-min: 6rem;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(var(--sg-swatch-min), 1fr));
    gap: var(--space-m);
  }

  .sg-swatch-grid :global(.color-swatch) {
    aspect-ratio: 1;
  }

  /* Light/dark comparison: two themed columns side by side. */
  .sg-theme-pair {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(15rem, 100%), 1fr));
    gap: var(--space-s);
  }

  .sg-theme-col {
    padding: var(--space-s);
    border: var(--border-width-hairline) solid var(--color-border);
    border-radius: var(--radius-sm);
  }

  .sg-theme-label {
    text-transform: uppercase;
    font-weight: var(--font-weight-medium);
  }

  /* Fixed-size swatch used inside the semantic table cells. */
  .sg-mini-swatch {
    display: inline-block;
    width: 2.5rem;
    height: 2.5rem;
    vertical-align: middle;
  }

  /* Border widths preview */
  .border-width-preview {
    border-bottom-style: solid;
    border-bottom-color: var(--color-accent);
  }

  /* Border radii preview */
  .border-radius-preview {
    width: 5rem;
    height: 3rem;
    background-color: var(--color-accent);
  }

  /* Shadows preview */
  .shadow-preview-container {
    background-color: var(--color-background);
    padding: var(--space-2xs);
  }

  .shadow-preview {
    width: 5rem;
    height: 3rem;
    background-color: var(--color-background);
  }

  /* Typeface specimens stack */
  .sg-type-specimens {
    display: flex;
    flex-direction: column;
    gap: var(--space-m);
  }

  /* Font sizes preview */
  .font-size-preview {
    color: var(--color-accent);
    font-family: var(--font-prose);
    white-space: nowrap;
  }

  /* Spacing scale preview */
  .spacing-bar {
    height: var(--space-s);
    background-color: var(--color-accent);
    border-radius: var(--radius-xs);
  }

  /* Font weights preview */
  .font-weight-preview {
    font-size: var(--font-size-lg);
    font-family: var(--font-prose);
    white-space: nowrap;
  }

  /* Line height preview */
  .leading-preview {
    max-width: 30ch;
  }

  /* Letter spacing preview */
  .letter-spacing-preview {
    font-family: var(--font-ui);
    text-transform: uppercase;
    font-feature-settings: 'case' on;
  }
</style>
