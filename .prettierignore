# Prettier ignores node_modules by default and respects .gitignore (dist,
# .astro, test-results, .unlighthouse, logs, etc.), so only patterns not
# already covered by those are listed here.

# Content — prose managed manually
*.md
*.mdx
